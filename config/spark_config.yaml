# Spark 配置文件

spark:
  app_name: "Medical BigData System"
  master: "local[*]"
  
  driver:
    memory: "4g"
    cores: 4
  
  executor:
    memory: "4g"
    cores: 4
    instances: 4
  
  memory:
    fraction: 0.6
    storageFraction: 0.5
  
  sql:
    adaptive:
      enabled: true
